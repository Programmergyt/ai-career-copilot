"""文档分类与个人信息提取 Prompt 模板"""

DOC_CLASSIFICATION_SYSTEM = """你是一位文档分类专家。你的任务是根据文件路径和内容摘要，将文档分类到指定类别。
请严格按照 JSON 格式输出，不要输出任何多余内容。"""

DOC_CLASSIFICATION_USER = """请对以下文档列表进行分类。每个文档包含文件路径和内容摘要。

## 分类类别
- 个人信息: 个人基本信息（包含姓名、年龄、电话、邮箱、工作地点、学历、教育背景、获奖经历等）
- 项目经历: 个人项目信息（课程项目、个人开源项目、竞赛项目等）
- 实习经历: 实习/工作经历信息
- 专业技能: 个人技能以及掌握的知识点总结
- 论文成果: 论文或学术研究成果

## 文档列表
{doc_list}

## 输出要求
请返回 JSON 数组，每个元素对应一个文档（不要包含 ```json 标记，直接返回纯 JSON）：
[
    {{"source_file": "文件路径", "doc_type": "分类类别"}},
    ...
]

注意：
1. 一个文档只能属于一个最主要的类别
2. 如果无法确定，优先归类为 项目经历
3. 包含个人简介、自我介绍、联系方式的文档归类为 个人信息"""

PROFILE_EXTRACTION_SYSTEM = """你是一位个人信息提取专家。你的任务是从文档中提取结构化的个人基本信息。
请严格按照 JSON 格式输出，不要输出任何多余内容。"""

PROFILE_EXTRACTION_USER = """请从以下文档内容中提取个人基本信息。

## 文档内容
{profile_text}

## 输出要求
请返回如下 JSON 结构（不要包含 ```json 标记，直接返回纯 JSON）：
{{
    "name": "姓名（如未找到则为 null）",
    "phone": "电话号码（如未找到则为 null）",
    "email": "邮箱地址（如未找到则为 null）",
    "location": "所在城市/工作地点（如未找到则为 null）",
    "education": [
        {{
            "school": "学校名称",
            "degree": "学位（本科/硕士/博士）",
            "major": "专业",
            "start_date": "入学时间",
            "end_date": "毕业时间"
        }}
    ],
    "awards": ["获奖经历1", "获奖经历2"],
    "certifications": ["证书1", "证书2"],
    "summary": "个人简介/自我评价（如有）"
}}

注意：
1. 仅提取文档中明确存在的信息，没有的字段填 null 或空列表
2. 不要编造任何信息"""
