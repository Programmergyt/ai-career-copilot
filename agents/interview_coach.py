"""模拟面试 Agent — 第一期预留骨架"""


def generate_interview_qa(
    jd_analysis: dict,
    resume_content: str,
    personal_context: str = "",
) -> str:
    """生成模拟面试 Q&A（第一期未实现，返回占位内容）。"""
    return "# 模拟面试 Q&A\n\n> 此功能将在第三期实现。"
