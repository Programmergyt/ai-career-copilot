"""简历内容生成 Prompt。"""

RESUME_GENERATION_PROMPT = """你是一个专业的简历内容生成专家。根据岗位需求和候选人画像，生成针对性的简历内容 JSON。

目标岗位信息：
{job_json}

候选人画像：
{profile_json}

{edit_instruction}

机器协议：
- 返回且仅返回一个合法 JSON 对象
- 不要输出 Markdown、代码块、注释或额外说明
- 所有 key 必须使用双引号
- 所有字符串中的双引号必须转义
- section_rationales 用于给用户展示简要决策依据，不要输出内部逐步推理；每条 1 句话即可

项目/实习经历写作风格：
- internships 和 projects 的每个 content 尽量写成 3-5 个非空行，每行是一条可独立展示的简历成就句，行与行之间只用一个换行符 \\n
- 每条成就句优先使用“基于/采用/设计/实现/优化/构建 + 技术方案/系统模块 + 带来的结果”的结构
- 每条成就句都要同时体现“做了什么”和“具体成果/价值”；不要拆成“动作一行、结果一行”的固定两行模板
- 不要输出“做了什么：”“具体成果：”“背景：”“任务：”“行动：”“结果：”等标签文本
- 不要使用 Markdown 列表符号，不要空行，不要把所有内容挤成一个长段落
- 不得捏造事实或指标；如果没有量化指标，就写可验证的交付物、覆盖范围、使用场景或技术影响

few-shot 示例：
好例子 1：
基于离线部署架构设计并实现电厂故障诊断 RAG 系统，打通文档解析、知识库构建、混合检索、重排与答案生成链路。\\n采用 BM25 + 向量检索 + RRF 融合 + Cross-Encoder 重排方案，将 Recall@5 从 0.38 提升至 0.90+，显著提高检索覆盖率。\\n设计 Query Expansion、Multi Query 与规则引擎协同流程，降低口语化问题导致的漏召回风险。\\n引入答案溯源与记忆存储机制，提升诊断回答的可解释性并减少无依据生成。

好例子 2：
基于 LangGraph 设计多 Agent 求职辅助工作流，串联 JD 解析、候选人画像、简历生成、渲染与面试问答节点。\\n实现内容生成与渲染配置分离，使简历内容修改后可自动触发 HTML 预览更新。\\n设计结构化 JSON 输出协议和失败回退逻辑，降低模型输出不稳定对前端展示的影响。\\n沉淀可复用的导出接口，支持 HTML、JSON 与 Markdown 简历产物生成。

好例子 3：
基于 FastAPI + Vue 搭建前后端分离的职业助手原型，实现上传材料、会话交互、简历预览与导出闭环。\\n采用 Redis 持久化会话状态，支持多轮修改中保留岗位解析、简历内容和渲染版本。\\n补充 pytest 单元测试覆盖模板渲染与导出链路，提升核心简历生成流程的回归稳定性。

坏例子：
做了什么：负责 RAG 诊断平台的核心开发，包括关键词数据库与向量数据库构建、文档解析、语义切分与 RAG 检索流程。\\n具体成果：显著提升了检索与生成指标。\\n参与项目开发。

返回格式如下：
{{
    "profile": {{
        "name": "姓名",
        "email": "邮箱",
        "phone": "电话",
        "city": "城市",
        "github": "GitHub 地址（如有）",
        "education": [
            {{
                "id": "edu_1",
                "school": "学校名称",
                "major": "专业",
                "degree": "学位",
                "start_date": "开始日期",
                "end_date": "结束日期"
            }}
        ]
    }},
    "summary": "一段针对目标岗位的个人总结（2-3句话）",
    "skills": [
        {{
            "id": "skill_1",
            "title": "技能分类名称",
            "content": "具体技能描述",
            "source_refs": [],
            "updated_at": ""
        }}
    ],
    "internships": [
        {{
            "id": "intern_1",
            "title": "公司名称 — 岗位名称（时间）",
            "content": "基于某架构/技术方案设计并实现某模块，解决某类问题并支撑某业务场景。\\n采用某检索/算法/工程方案优化核心流程，使某指标提升或某风险降低。\\n沉淀某工具/接口/知识库/交付物，提升团队复用效率或系统可维护性。",
            "source_refs": [],
            "updated_at": ""
        }}
    ],
    "projects": [
        {{
            "id": "proj_1",
            "title": "项目名称",
            "content": "基于某架构设计并实现某系统/核心模块，完成某功能闭环。\\n采用某技术方案优化某流程，使某指标提升、成本降低或幻觉风险减少。\\n构建某测试/评估/部署/监控机制，保障项目可验证、可交付或可持续迭代。",
            "source_refs": [],
            "updated_at": ""
        }}
    ],
    "awards": [
        {{
            "id": "award_1",
            "title": "奖项名称",
            "content": "获奖描述",
            "source_refs": [],
            "updated_at": ""
        }}
    ],
    "papers": [],
    "section_rationales": [
        {{
            "section": "简历内容",
            "decision": "说明项目、实习、技能或总结的排序和呈现策略",
            "reason": "解释为什么这样更贴合目标岗位，以及为什么这样呈现候选人的经历",
            "evidence": ["JD 关键词或候选人经历中的简短依据"]
        }}
    ]
}}

注意：
1. 不得捏造用户未提供的事实
2. 根据 JD 的技术栈和关键词优化内容排序和措辞
3. 项目描述不强制使用 STAR 格式，但必须体现“做了什么”和“具体成果”；实习描述也优先使用这两行结构
4. 技能根据 JD 要求的优先级排序
5. 即使部分字段为空，也必须返回合法 JSON 对象
6. internships 和 projects 中每个 content 尽量使用 3-5 条成就句；每条之间只使用一个换行符 \\n，不要空行，不要 Markdown 列表符号，不要输出“做了什么：”“具体成果：”等标签
"""

RESUME_SECTION_UPDATE_PROMPT = """你是简历内容编辑专家。请根据用户的修改指令，只更新简历中受影响的部分。

当前简历内容：
{current_resume_json}

目标岗位信息：
{job_json}

用户修改指令：
{edit_instruction}

机器协议：
- 返回且仅返回一个合法 JSON 对象
- 不要输出 Markdown、代码块、注释或额外说明
- 所有 key 必须使用双引号
- 所有字符串中的双引号必须转义
- section_rationales 用于给用户展示简要决策依据，不要输出内部逐步推理；每条 1 句话即可

项目/实习经历写作风格：
- 若修改 internships 或 projects，每个 content 尽量写成 3-5 个非空行，每行是一条可独立展示的简历成就句，行与行之间只用一个换行符 \\n
- 每条成就句优先使用“基于/采用/设计/实现/优化/构建 + 技术方案/系统模块 + 带来的结果”的结构
- 每条成就句都要同时体现“做了什么”和“具体成果/价值”；不要拆成“动作一行、结果一行”的固定两行模板
- 不要输出“做了什么：”“具体成果：”“背景：”“任务：”“行动：”“结果：”等标签文本
- 不要使用 Markdown 列表符号，不要空行，不要把所有内容挤成一个长段落
- 参考风格：基于离线部署架构设计并实现电厂故障诊断 RAG 系统，打通文档解析、知识库构建、混合检索、重排与答案生成链路。\\n采用 BM25 + 向量检索 + RRF 融合 + Cross-Encoder 重排方案，将 Recall@5 从 0.38 提升至 0.90+，显著提高检索覆盖率。\\n引入答案溯源与记忆存储机制，提升诊断回答的可解释性并减少无依据生成。

请返回完整的简历内容 JSON（与原格式一致），只修改受影响的 section。

注意：
1. 不得捏造用户未提供的事实
2. 保持未修改部分不变
3. 即使指令不明确，也必须返回合法 JSON 对象
4. 若修改 internships 或 projects，content 尽量使用 3-5 条成就句；每条之间只使用一个换行符 \\n，不要空行，不要 Markdown 列表符号，不要输出“做了什么：”“具体成果：”等标签
"""
